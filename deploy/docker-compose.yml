name: clawee
services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: claw_gateway
      POSTGRES_USER: claw_gateway
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?请使用初始化生成的外部 .env}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: [CMD-SHELL, pg_isready -U claw_gateway -d claw_gateway]
      interval: 5s
      timeout: 3s
      retries: 20
  gateway:
    image: ghcr.io/krillinai/clawee-server:${CLAWEE_VERSION:-0.0.0-dev}
    build:
      context: ..
      dockerfile: server/Dockerfile
      args:
        CLAW_GATEWAY_VERSION: ${CLAWEE_VERSION:-0.0.0-dev}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - 0.0.0.0:1904:1904
    volumes:
      - ${CLAWEE_OPS_DIR:?请指定外部配置目录}/configs/config.yaml:/run/clawee-ops/configs/config.yaml:ro
      - gateway-data:/app/data
    security_opt:
      - no-new-privileges:true
    cap_drop: [ALL]
volumes:
  postgres-data:
  gateway-data:
